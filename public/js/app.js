/* 
	This is the main configuration file that tells our project that it's using angular 
	It is also used to name your app and match it to the HTML code (in this case it's called
	"dashboard").
*/

window.app = angular.module('dashboard', []); 
window.app.routeStore = '';